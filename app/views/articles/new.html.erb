<%= form_with(model: @article, url: articles_path, method: :post) do |f| %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, autofocus: true, autocomplete: "title" %>
  </div>

    <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, autocomplete: "body" %>
  </div>

     <div class="field">
    <%= f.label :category %><br>
    <%= f.text_area :category, autocomplete: "category" %>
  </div>

  <div class="field">
    <%= f.submit %>
  </div>

<% end %>